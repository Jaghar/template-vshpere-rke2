rancher_min_version: 2.7.9
questions:
#- variable: cluster.name
 # description: "Specify name of the cluster"
 # label: "Cluster Name"
#  required: true
#  type: string
#  group: "General"

- variable: cluster.name
  default: rke2
  description: "Specify the name of the cluster"
  label: "Cluster Name"
  required: true
  type: string
  group: "General"

- variable: vCenterPassword
  description: "Specify vCenter passwor for CSI/CPI"
  label: "vCenter Password"
  required: true
  type: password
  group: "General"

- variable: cloudCredentialSecretName
  description: "Choose CloudCredential for cluster provisiong"
  label: "CloudCredential Name"
  required: true
  type: cloudcredential
  group: "General"

- variable: kubernetesVersion
  description: "Specify Kubernetes version"
  label: "Kubernetes Version"
  required: true
  default: v1.26.13+rke2r1
  type: enum
  options:
  - v1.26.13+rke2r1
  - v1.25.7-rke2r1
  - v1.24.9-rke2r2
  - v1.24.8-rke2r1
  group: "General"

- variable: cniVersion
  description: "Specify Kubernetes CNI"
  label: "Kubernetes CNI"
  required: true
  default: canal
  type: enum
  options:
  - calico
  - cilium
  - canal
  - none
  group: "General"

- variable: s3.cloudCredential
  description: "Choose CloudCredential for Etcd S3 Backup"
  label: "CloudCredential Name"
  required: true
  type: cloudcredential
  group: "General"

- variable: s3.bucket
  description: "Specify S3 Bucket name"
  label: "S3 bucket name"
  required: true
  default: buc-cloe-backup-k8s-dev-ppd
  type: enum
  options:
  - buc-cloe-backup-k8s-dev
  - buc-cloe-backup-k8s-qua
  - buc-cloe-backup-k8s-otl
  - buc-cloe-backup-k8s-adm
  - buc-cloe-backup-k8s-dev-ppd
  - buc-cloe-backup-k8s-qua-ppd
  - buc-cloe-backup-k8s-otl-ppd
  group: "General"


- variable: pool.controlplane.vmbasket
  description: "Size of VMs in pool"
  label: "Desired size of VM"
  required: true
  default: "SMALL -> vCPU:2 / MEM:8GB"
  type: enum
  options:
  - TINY -> vCPU:2 / MEM:8GB
  - SMALL -> vCPU:2 / MEM:8GB
  - STANDARD -> vCPU:4 / MEM:16GB
  group: "Nodepool: controlplane"

- variable: pool.controlplane.vlan
  description: "vSphere VLAN name for nodepool VMs"
  label: "Nodepool VLAN name"
  required: true
  default: "VMNetworkNAME -> NETWORK_DESCRIPTION"
  type: enum
  options:
  - VMNetworkNAME -> NETWORK_DESCRIPTION
  - VMNetworkNAME_OTHER -> NETWORK_DESCRIPTION
  group: "Nodepool: controlplane"

- variable: pool.controlplane.quantity
  label: "Number of instance"
  default: 3
  description:
  type: enum
  required: true
  options:
  - 1
  - 3
  - 5
  group: "Nodepool: controlplane"   

- variable: pool.worker.vmbasket
  description: "Size of VMs in pool"
  label: "Desired size of VM"
  required: true
  default: "SMALL -> vCPU:4 / MEM:16GB"
  type: enum
  options:
  - TINY -> vCPU:4 / MEM:16GB
  - SMALL -> vCPU:4 / MEM:16GB
  - STANDARD -> vCPU:8 / MEM:64GB
  group: "Nodepool: worker"
  
- variable: pool.worker.vlan
  description: "vSphere VLAN name for nodepool VMs"
  label: "Nodepool VLAN name"
  required: true
  default: "VMNetworkNAME -> NETWORK_DESCRIPTION"
  type: enum
  options:
  - VMNetworkNAME -> NETWORK_DESCRIPTION
  - VMNetworkNAME_OTHER -> NETWORK_DESCRIPTION
  group: "Nodepool: worker"

- variable: pool.worker.quantity
  label: "Number of instance"
  default: 2
  description:
  type: enum
  required: true
  options:
  - 2
  - 4
  - 6
  group: "Nodepool: worker"
